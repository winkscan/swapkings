// Design-review-only fake data — RankCard's demo state, so its layout can be
// polished without a connected wallet. Flip DEMO_MODE off once it's no
// longer needed.
export const DEMO_MODE = false

export const MOCK_ALL_TIME_VOLUME = 612480.35

export const MOCK_RANK = {
  volumeUsd: 184200,
  tier: { name: 'Veteran', emoji: '🛡️' },
  discountBps: 2_500,
}
